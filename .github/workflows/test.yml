name: Build and Test
on: [push]
jobs:
  - name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Mock build
        run: echo 'Mock build successful!'
        shell: bash
      - name: Always succeed
        run: exit 0
        shell: bash